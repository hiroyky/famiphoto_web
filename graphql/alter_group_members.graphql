mutation alterGroupMembers($groupId: ID!, $appendUserIds: [ID!], $removeUserIds: [ID!]) {
  alterGroupMembers(input: {
    groupId: $groupId,
    appendUserIds: $appendUserIds,
    removeUserIds: $removeUserIds
  }) {
    id
  }
}
