#codegen.yaml

schema: 'http://localhost:7002/graphql'
documents: './graphql/*.graphql'
generates:
  types/api-gql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
